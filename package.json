{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.2"
  },
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "deploy": "lerna run deploy",
    "deploy:studio": "lerna run deploy --scope=studio",
    "deploy:homepage": "lerna run deploy --scope=homepage"
  }
}
